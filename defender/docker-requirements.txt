# Web app / server
flask>=1.1.2
gevent>=1.4.0
envparse

# ML runtime
numpy>=1.24
lightgbm>=4.0

# PE parsing (used by EMBER2024 v3 "thrember")
pefile>=2023.2.7

# EMBER2024 feature extractor (installs the `thrember` package)
# Pin to a commit for reproducibility if you can:
git+https://github.com/FutureComputing4AI/EMBER2024.git@main
